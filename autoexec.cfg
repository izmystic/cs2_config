alias eae "exec autoexec"
r_show_build_info 0

// KEYBOARD
    // NUMERIC
        bind 1 "slot1"
        bind 2 "slot2"
        bind 3 "slot3"
        bind 4 "slot4"
        bind 5 "slot5"
        bind 8 "invnext"
        bind 9 "invprev"

    // MOVEMENT
        bind w "+forward;colors"
        bind a "+left;colors"
        bind s "+back;colors"
        bind d "+right;colors"

        bind space "+jump"
        bind ctrl "+duck;colors"
        bind shift "+sprint;colors"

        bind mwheelup ""
        bind mwheeldown "+jump"

    // ARROWS
        bind leftarrow "+turnleft"
        bind rightarrow "+turnright"

        // 0.7 sens 180 binds: 180 ÷ (0.022 × sens) = yaw
        bind uparrow "yaw 11688 1 1"
        bind downarrow "yaw -11688 1 1"

    // MISCELLANEOUS
        bind x "use_skill;slot12"

// RAINBOW HUD
        alias colors "incrementvar cl_hud_color 4 10 1"

// OTHER
    // VIEWMODEL
        viewmodel_fov "68"
        viewmodel_offset_x "2.5"
        viewmodel_offset_y "2.0"
        viewmodel_offset_z "-2.0"
        viewmodel_presetpos "0"

    // MINIMAP
        cl_radar_always_centered "0"
        cl_radar_scale "0.75"
        cl_radar_icon_scale_min "0.4"

    // SENS
        sensitivity 0.7
        zoom_sensitivity_ratio 0.8
